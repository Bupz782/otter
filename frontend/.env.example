# Base URL of the Otter API. Leave empty to proxy through the same origin in dev.
VITE_API_URL=http://localhost:3001

# Optional. HTTP endpoint that collects waitlist emails via POST {"email": ...}
# (e.g. Formspree, Buttondown, Loops). Leave unset to simulate submissions locally.
VITE_WAITLIST_ENDPOINT=
